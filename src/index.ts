import { suma, multiplicar, saludar } from './utils';

// eslint-disable-next-line no-console
console.log(saludar('DevSecOps'));
// eslint-disable-next-line no-console
console.log('2 + 3 =', suma(2, 3));
// eslint-disable-next-line no-console
console.log('4 × 5 =', multiplicar(4, 5));
